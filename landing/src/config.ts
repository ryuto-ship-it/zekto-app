// Standalone copy of ../../src/config/links.ts — this landing page is a
// separate Vite project (not part of the RN Expo bundle), so it keeps its
// own copy rather than reaching across the package boundary. Keep both in
// sync when a URL changes.
export const APP_DEMO_URL = 'https://ryuto-ship-it.github.io/zekto-app/';

// Placeholder Google Docs link — swap for the real public whitepaper URL
// once it's published.
export const WHITEPAPER_URL = 'https://docs.google.com/document/d/PLACEHOLDER-ZEKTO-WHITEPAPER/edit';
